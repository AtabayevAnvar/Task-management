# PostgreSQL ulanish (majburiy)
DATABASE_URL=postgresql://foydalanuvchi:parol@localhost:5432/taskflow

# JWT (productionda kuchli qiymat ishlating)
JWT_SECRET=ozgartiring_bu_qatorni
JWT_EXPIRES_IN=24h

# Server porti (ixtiyoriy, standart: 3000)
PORT=3000

# Render / bulut PostgreSQL uchun SSL (kerak bo'lsa qo'shing)
# PGSSLMODE=require
# DB_SSL=true
